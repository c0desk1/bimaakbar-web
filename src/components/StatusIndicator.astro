---
// src/components/StatusIndicator.astro
import { SITE } from "../consts"
import Logo from './Logo';
---
<section class="mx-auto items-center w-screen px-22 py-6 border-t border-[var(--color-border)]">
  <div class="flex flex-col md:flex-row items-center font-semibold gap-1 ">
    <a href="/" class="flex flex-1 justify-start gap-1 items-center text-center text-current hover:text-[var(--color-fg)]">
      <Logo width={32} height={32} />
      <span>{SITE.TITLE}</span>
    </a>
    <div class="flex flex-row flex-wrap justify-end gap-1.5 items-center text-center text-current hover:text-[var(--color-fg)]">
      <span class="relative flex h-3 w-3">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[var(--color-accent)] opacity-50"></span>
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full border border-[var(--color-accent)] opacity-50"></span>
        <span class="relative inline-flex rounded-full h-3 w-3 bg-[var(--color-accent)] opacity-75"></span>
      </span>
      <p class="font-medium text-[var(--color-muted)]">
        All systems normal.
      </p>
    </div>
  </div>
</section>